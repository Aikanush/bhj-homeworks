const menu = document.querySelector('.dropdown');


menu.addEventListener('click', function(event) {
	let target = event.target;

	let targetParent = target.closest('.dropdown__item');

	if(targetParent) {

	}

})